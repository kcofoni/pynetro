# Development dependencies for pynetro

# Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-cov>=4.0.0

# HTTP client for integration tests
aiohttp>=3.8.0

# Parallel testing (optional)
pytest-xdist>=3.0.0

# Linting (already included in pyproject.toml but can be installed separately)
ruff>=0.1.0

# Automatic environment variable loading
python-dotenv>=1.0.0